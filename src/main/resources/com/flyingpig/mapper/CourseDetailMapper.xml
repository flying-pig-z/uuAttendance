<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.flyingpig.mapper.CourseDetailMapper" >
    <select id="selectCourseIdByBeginTimeAndTeacherid" resultType="integer">
        select id from course_detail where course_teacher =#{teacherId} and week=#{week} and weekday=#{weekday} and section=#{section}
    </select>

</mapper>